# Optional. Open-Meteo and NOAA require no key, so the weather agent works without this.
# Only needed if you want WeatherAPI.com as an additional fallback source.
WEATHER_API_KEY=

# Optional. Enables the sportsdata.io stats fallback in utils/api_clients.py.
# ESPN and Pro Football Reference are tried first and need no key.
NFL_API_KEY=
